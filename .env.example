# Create .env.local from this file and use one of the two TMDB credentials below.
# The read-access token is preferred and must never be prefixed with NEXT_PUBLIC_.
TMDB_READ_ACCESS_TOKEN=
# Alternative name supported for existing projects.
TMDB_ACCESS_TOKEN=
TMDB_API_KEY=
